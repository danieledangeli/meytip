{% extends '::base.html.twig' %}

{% block body -%}
    <h1>QuoteFinale</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Podio</th>
                <td>{{ entity.podio }}</td>
            </tr>
            <tr>
                <th>Nopodio</th>
                <td>{{ entity.nopodio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('quotefinale') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('quotefinale_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('quotefinale_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
